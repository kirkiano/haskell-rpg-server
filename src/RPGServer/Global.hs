{-# OPTIONS_GHC -Wall #-}

module RPGServer.Global ( module RPGServer.Global.Env,
                          module RPGServer.Global.Settings ) where

import RPGServer.Global.Env
import RPGServer.Global.Settings
